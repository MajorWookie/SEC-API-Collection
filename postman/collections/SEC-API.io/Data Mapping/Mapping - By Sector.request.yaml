$kind: http-request
description: >-
  List all companies in a specific sector.


  **Path Parameters:**

  - `sector` (required): Industry sector name (e.g. Technology, Healthcare,
  Finance)


  **Docs:** https://sec-api.io/docs/mapping-api
url: "{{BASE_URL}}/mapping/sector/Technology?token={{SEC_API_KEY}}"
method: GET
queryParams:
  - key: token
    value: "{{SEC_API_KEY}}"
    description: API key
order: 6000
