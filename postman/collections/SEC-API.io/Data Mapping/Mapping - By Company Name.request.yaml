$kind: http-request
description: |-
  Search companies by name.

  **Path Parameters:**
  - `name` (required): Company name or partial name

  **Docs:** https://sec-api.io/docs/mapping-api
url: "{{BASE_URL}}/mapping/name/Apple?token={{SEC_API_KEY}}"
method: GET
queryParams:
  - key: token
    value: "{{SEC_API_KEY}}"
    description: API key
order: 4000
